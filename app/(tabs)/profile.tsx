import { SafeAreaView , Text } from "react-native"

const Profile = () => {
return(
    <SafeAreaView>
        <Text className='flex bottom-1'>
            Hay
        </Text>
    </SafeAreaView>
)
}

export default Profile